public class Task01 {
    public static void main(String[] args) {

        //Типы переменных в switch:

        switch (intValue) {} //`int`
        switch (charValue) {} //`char`
        switch (stringValue) {} //`String`
        switch (enumValue) {} //`enum`
        switch (byteValue) {} //`byte`
        switch (shortValue) {} //`short`
        // У starting с Java 7 `String` и enum.
    }
}
