public class Task02 {
    public static void main(String[] args) {
        System.out.println("Если метод `method()` класса `C` вызывает `super.method()`, он вызовет метод `method()` из `A`. Переменная `a` также будет взята из `A`, так как она не определена в `B`.");
        //Если метод `method()` класса `C` вызывает `super.method()`, он вызовет метод `method()` из `A`. 
        //Переменная `a` также будет взята из `A`, так как она не определена в `B`.
    }
}
